name = "smart-gateway"
main = "src/index.js"
compatibility_date = "2024-09-23"

# --- AI binding (Workers AI - no API key needed) ---
[ai]
binding = "AI"

# --- Backend URL ---
# Use your public backend URL. If behind a tunnel, use the tunnel hostname.
[vars]
BACKEND_URL = "http://naturalgate.mvplab.space:8080"
